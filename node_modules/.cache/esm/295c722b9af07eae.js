"main";let fs;_9af‍.w("fs",[["default",["fs"],function(v){fs=v}]]);let connectDb,clearDatabase;_9af‍.w("./database.js",[["connectDb",["connectDb"],function(v){connectDb=v}],["clearDatabase",["clearDatabase"],function(v){clearDatabase=v}]]);



function setupDb(){
    const db = connectDb()
    clearDatabase(db)
    
    
    fs.readdir('exchanges', async (err,moduleFiles)=>{
        
        let exchangeModules = {}
        for (const moduleFile of moduleFiles){
            const exchangeName = moduleFile.split('.')[0]
            exchangeModules[exchangeName] = require(`./exchanges/${moduleFile}`)
        }
        
        let markets = {}
        for (let exchangeModuleName of Object.keys(exchangeModules)){
            const module = await exchangeModules[exchangeModuleName][exchangeModuleName]
            // console.log(module)
            const markets = await module.fetchMarkets()
            _9af‍.g.console.log(markets)
            for (let market of markets){
                // console.log(markets)
            }
        }
    
    })
}


setupDb()