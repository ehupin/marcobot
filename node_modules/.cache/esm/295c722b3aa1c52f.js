"main";let fs;_3aa‍.w("fs",[["default",["fs"],function(v){fs=v}]]);let connectDb,clearDatabase;_3aa‍.w("./database.js",[["connectDb",["connectDb"],function(v){connectDb=v}],["clearDatabase",["clearDatabase"],function(v){clearDatabase=v}]]);





const db = connectDb()
clearDatabase(db)


fs.readdir('exchanges', (err,moduleFiles)=>{
    let exchangeModules = {}
    for (const moduleFile of moduleFiles){
        const exchangeName = moduleFile.split('.')[0]
        exchangeModules[exchangeName] = require(`./exchanges/${moduleFile}`)
    }
    _3aa‍.g.console.log(exchangeModules)
    let markets = {}
    for (let exchangeModuleName of Object.keys(exchangeModules)){
        exchangeModules[exchangeModuleName][exchangeModuleName].fetchMarkets().then(markets=>{
            _3aa‍.g.console.log(markets)
        })
    }
    // bittrex.bittrex.fetchMarkets()

})