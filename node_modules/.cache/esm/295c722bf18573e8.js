"main";let fs;_f18‍.w("fs",[["default",["fs"],function(v){fs=v}]]);let connectDb,clearDatabase;_f18‍.w("./database.js",[["connectDb",["connectDb"],function(v){connectDb=v}],["clearDatabase",["clearDatabase"],function(v){clearDatabase=v}]]);



async function setupDb(){
    const db = connectDb()
    clearDatabase(db)
    
    
    fs.readdir('exchanges', (err,moduleFiles)=>{
        let exchangeModules = {}
        for (const moduleFile of moduleFiles){
            const exchangeName = moduleFile.split('.')[0]
            exchangeModules[exchangeName] = require(`./exchanges/${moduleFile}`)
        }
        _f18‍.g.console.log(exchangeModules)
        let markets = {}
        for (let exchangeModuleName of Object.keys(exchangeModules)){
            
            const markets = exchangeModules[exchangeModuleName][exchangeModuleName]
                                .fetchMarkets()
            for (let market of markets){
                _f18‍.g.console.log(markets)
            }
        }
        // bittrex.bittrex.fetchMarkets()
    
    })
}


